import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.mide.ionic.pushnotifikationen',
  appName: 'Pushnotifikationen',
  webDir: 'www'
};

export default config;
